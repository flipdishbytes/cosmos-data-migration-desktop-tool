# Copy to .env and fill in your values. Used by run-full-sync.sh and run-backfill.sh.
# Do not commit .env (it is in .gitignore).
# IMPORTANT: Values MUST be quoted because connection strings contain semicolons.

STORAGE_CONNECTION_STRING="DefaultEndpointsProtocol=https;AccountName=<storage-account>;AccountKey=<key>;EndpointSuffix=core.windows.net"
COSMOS_CONNECTION_STRING="AccountEndpoint=https://<account>.table.cosmos.azure.com;AccountKey=<key>;"
